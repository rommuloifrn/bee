#include <stdio.h>

int main() {
    unsigned int rgb;
    unsigned char r, g, b, alfa;
    scanf("%u", &rgb);
    printf("RGB = %08X\n", rgb);

    /* Realizar conversões para as variáveis r, g, b e alfa */
    r
    return 0;
}